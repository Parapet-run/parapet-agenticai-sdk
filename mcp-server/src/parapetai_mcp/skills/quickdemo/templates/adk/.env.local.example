# Copy to .env for LOCAL mode (no control plane at all: `cp .env.local
# .env`). Cedar policies are read straight from ./policies/ on disk --
# see README.md's "Switching between cloud and local mode". Nothing here
# needs a real agent_id/secret; this mode never makes a network call to
# the control plane.

PARAPETAI_MODE=local

# --- Model: mock by default -------------------------------------------
# Leave GOOGLE_API_KEY unset to use the built-in local mock model
# (mock_llm.py, no network call, fully deterministic). Set a real Gemini
# Developer API key here (https://aistudio.google.com/apikey) to use a
# real model instead; the agent code is identical either way.
# GOOGLE_API_KEY=
# GOOGLE_MODEL=gemini-3.6-flash

# --- Local output (off by default, same in cloud or local mode) --------
# Every governed run is silent on stdout by default. Uncomment to print
# the structlog/OTel decision stream again while debugging a policy:
# PARAPETAI_CONSOLE_LOG=true
#
# Uncomment to also (or instead) write those decisions to a rotating
# JSON-lines file:
# PARAPETAI_LOCAL_LOG_DIR=./logs

# Nothing below this line is read in local mode -- ./policies/ is loaded
# directly instead. Left here, commented out, only so switching back with
# `cp .env.cloud .env` doesn't mean retyping real values.
# PARAPETAI_AGENT_ID=
# PARAPETAI_AGENT_SECRET=
# PARAPETAI_CONTROL_PLANE_URL=https://app.parapet.run
# PARAPETAI_ACCOUNT_ID=
