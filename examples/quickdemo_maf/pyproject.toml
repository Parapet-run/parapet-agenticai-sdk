[project]
name = "parapet-quickdemo-maf"
version = "0.1.0"
description = "Parapet quickdemo: Sales vs HR identity-based tool access, Microsoft Agent Framework"
requires-python = ">=3.12"
dependencies = [
    "parapetai-agent[maf]>=0.3,<1.0",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
